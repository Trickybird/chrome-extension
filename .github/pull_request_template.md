## What was wrong

<!-- The problem, not the diff. What did it do that it should not have, or fail to do? -->

## Why this fixes it

<!-- The reasoning. If a reviewer has to derive it from the code, it belongs here instead. -->

## The test that catches it

<!-- Name the break it would catch. If you cannot, say why this change needs no test. -->

## Output

```
$ npm run check

```

## Permissions

- [ ] The manifest's permission set is unchanged.
- [ ] Or: it changed, `permissions.baseline.json` was regenerated, and the description above says
      what the permission is for and why the feature cannot work without it.
