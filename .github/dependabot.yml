version: 2
updates:
  # The workflow pins actions by commit, so this is what moves them.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: monthly
