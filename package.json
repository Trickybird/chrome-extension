{
  "name": "trickybird-extension",
  "version": "1.2.4",
  "private": true,
  "description": "Chrome extension that routes one browser tab through the TrickyBird web proxy.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "types": "tsc --noEmit",
    "permissions": "node tools/check-permissions.mjs",
    "package": "node tools/package.mjs",
    "check": "npm run types && npm test && npm run permissions"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.9",
    "@types/node": "^26.5.1",
    "typescript": "^7.0.2"
  }
}
